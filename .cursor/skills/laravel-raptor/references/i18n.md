# Internacionalização — PT-BR e Laravel Translations

## Estrutura de arquivos

```
lang/
├── pt_BR/
│   ├── app.php           # strings comuns da aplicação
│   ├── auth.php          # autenticação
│   ├── pagination.php    # paginação
│   ├── validation.php    # validação de formulários
│   ├── passwords.php     # reset de senha
│   └── resources/
│       ├── products.php
│       ├── categories.php
│       ├── planograms.php
│       └── ...
```

---

## app.php — strings comuns

```php
<?php
// lang/pt_BR/app.php

return [
    // Navegação
    'dashboard'         => 'Painel de Controle',
    'back'              => 'Voltar',
    'home'              => 'Início',

    // Ações CRUD
    'create'            => 'Novo :resource',
    'edit'              => 'Editar :resource',
    'delete'            => 'Excluir :resource',
    'view'              => 'Visualizar :resource',
    'list'              => 'Listagem de :resource',
    'save'              => 'Salvar',
    'cancel'            => 'Cancelar',
    'confirm'           => 'Confirmar',
    'close'             => 'Fechar',
    'search'            => 'Buscar...',
    'filter'            => 'Filtros',
    'clear_filters'     => 'Limpar filtros',
    'export'            => 'Exportar',
    'import'            => 'Importar',
    'duplicate'         => 'Duplicar',
    'restore'           => 'Restaurar',
    'archive'           => 'Arquivar',

    // Confirmações
    'confirm_delete'         => 'Excluir :resource?',
    'confirm_delete_message' => 'Esta ação não pode ser desfeita. O :resource será excluído permanentemente.',
    'confirm_delete_button'  => 'Sim, excluir',

    // Estados
    'loading'    => 'Carregando...',
    'saving'     => 'Salvando...',
    'no_results' => 'Nenhum resultado encontrado.',
    'empty'      => 'Nenhum :resource cadastrado.',
    'empty_cta'  => 'Que tal criar o primeiro :resource?',

    // Status
    'status'     => 'Status',
    'active'     => 'Ativo',
    'inactive'   => 'Inativo',
    'draft'      => 'Rascunho',
    'published'  => 'Publicado',
    'archived'   => 'Arquivado',

    // Metadados
    'created_at' => 'Criado em',
    'updated_at' => 'Atualizado em',
    'created_by' => 'Criado por',
    'id'         => 'ID',
    'name'       => 'Nome',
    'description'=> 'Descrição',
    'status'     => 'Status',

    // Paginação
    'showing'    => 'Exibindo :from–:to de :total registros',
    'per_page'   => 'por página',
    'previous'   => 'Anterior',
    'next'       => 'Próximo',

    // Tema
    'light_mode' => 'Tema claro',
    'dark_mode'  => 'Tema escuro',
    'system_mode'=> 'Padrão do sistema',

    // Erros
    'error'          => 'Ocorreu um erro',
    'error_message'  => 'Algo deu errado. Tente novamente.',
    'unauthorized'   => 'Você não tem permissão para realizar esta ação.',
    'not_found'      => ':resource não encontrado.',
    'server_error'   => 'Erro interno do servidor.',

    // Notificações de sucesso
    'created_success'  => ':resource criado com sucesso!',
    'updated_success'  => ':resource atualizado com sucesso!',
    'deleted_success'  => ':resource excluído com sucesso!',
    'imported_success' => ':count :resource(s) importado(s) com sucesso!',
    'exported_success' => 'Exportação concluída.',

    // Perfil / usuário
    'profile'     => 'Meu Perfil',
    'settings'    => 'Configurações',
    'logout'      => 'Sair',
    'welcome'     => 'Olá, :name!',
];
```

---

## validation.php — mensagens de validação

```php
<?php
// lang/pt_BR/validation.php

return [
    'required'  => 'O campo :attribute é obrigatório.',
    'email'     => 'O campo :attribute deve ser um endereço de e-mail válido.',
    'min'       => [
        'string'  => 'O campo :attribute deve ter pelo menos :min caracteres.',
        'numeric' => 'O campo :attribute deve ser no mínimo :min.',
    ],
    'max'       => [
        'string'  => 'O campo :attribute deve ter no máximo :max caracteres.',
        'numeric' => 'O campo :attribute deve ser no máximo :max.',
        'file'    => 'O arquivo :attribute não pode ter mais de :max kilobytes.',
    ],
    'unique'    => 'Este :attribute já está em uso.',
    'exists'    => ':attribute inválido.',
    'numeric'   => 'O campo :attribute deve ser um número.',
    'integer'   => 'O campo :attribute deve ser um número inteiro.',
    'boolean'   => 'O campo :attribute deve ser verdadeiro ou falso.',
    'date'      => 'O campo :attribute não é uma data válida.',
    'after'     => 'O campo :attribute deve ser uma data posterior a :date.',
    'before'    => 'O campo :attribute deve ser uma data anterior a :date.',
    'image'     => 'O campo :attribute deve ser uma imagem.',
    'mimes'     => 'O campo :attribute deve ser um arquivo do tipo: :values.',
    'confirmed' => 'A confirmação do campo :attribute não corresponde.',
    'in'        => 'O valor selecionado para :attribute é inválido.',

    'attributes' => [
        'name'        => 'nome',
        'email'       => 'e-mail',
        'password'    => 'senha',
        'description' => 'descrição',
        'status'      => 'status',
        'price'       => 'preço',
        'quantity'    => 'quantidade',
        'category_id' => 'categoria',
        'created_at'  => 'data de criação',
        'updated_at'  => 'data de atualização',
        'start_date'  => 'data de início',
        'end_date'    => 'data de término',
    ],
];
```

---

## Integração Vue via Inertia

### HandleInertiaRequests.php

```php
// app/Http/Middleware/HandleInertiaRequests.php

public function share(Request $request): array
{
    return array_merge(parent::share($request), [
        'translations' => fn () => [
            'app'        => trans('app'),
            'validation' => trans('validation'),
            // Adicionar traduções do recurso atual se necessário
        ],
        'locale' => app()->getLocale(),  // 'pt_BR'
    ]);
}
```

### composable useT.ts

```ts
// composables/useT.ts
import { usePage } from '@inertiajs/vue3'

type Translations = Record<string, Record<string, string>>

export function useT() {
  const page = usePage()

  function t(key: string, replacements: Record<string, string | number> = {}): string {
    const translations = (page.props.translations as Translations) || {}
    const [group, ...rest] = key.split('.')
    const k = rest.join('.')

    let str: string = translations[group]?.[k] ?? key

    Object.entries(replacements).forEach(([find, replace]) => {
      str = str.replace(`:${find}`, String(replace))
    })

    return str
  }

  return { t }
}

// Uso:
// const { t } = useT()
// t('app.create', { resource: 'Produto' })  → "Novo Produto"
// t('app.deleted_success', { resource: 'Produto' })  → "Produto excluído com sucesso!"
```

---

## Correções gramaticais obrigatórias

Ao encontrar qualquer uma dessas strings no código (controllers, blade, vue), corrigir:

### Títulos de página
| ❌ Encontrado | ✅ Corrigido |
|--------------|-------------|
| `Gerenciar Planogram` | `Gerenciar Planogramas` |
| `Gerenciar Produto` | `Gerenciar Produtos` |
| `Listar` (como título) | `Listagem de [Recurso]` |
| `Dashboard` | `Painel de Controle` |
| `Criar Novo` | `Novo [Recurso]` |
| `Index` | `Listagem` |
| `Show` | `Detalhes` |
| `Edit` | `Editar` |
| `Create` | `Novo` / `Criar` |
| `Store` (título) | `Novo [Recurso]` |

### Campos e labels
| ❌ Encontrado | ✅ Corrigido |
|--------------|-------------|
| `Items` | `Itens` |
| `Nível Nome` | `Nome do Nível` |
| `Data Inicio` | `Data de Início` |
| `Data Termino` | `Data de Término` |
| `Prazo Termino` | `Prazo de Término` |
| `Criado Em` | `Criado em` |
| `Atualizado Em` | `Atualizado em` |
| `Codigo ERP` | `Código ERP` |

### Mensagens
| ❌ Encontrado | ✅ Corrigido |
|--------------|-------------|
| `Deletado com sucesso` | `Excluído com sucesso!` |
| `Salvo com sucesso` | `Salvo com sucesso!` |
| `Erro ao salvar` | `Erro ao salvar. Tente novamente.` |
| `Tem certeza?` | `Tem certeza que deseja excluir?` |

---

## config/app.php — Locale PT-BR

```php
// config/app.php
'locale' => 'pt_BR',
'fallback_locale' => 'en',
'faker_locale' => 'pt_BR',
'timezone' => 'America/Sao_Paulo',
```

---

## Formatação de datas e números — PT-BR

```ts
// utils/format.ts

export function formatDate(date: string | Date, options?: Intl.DateTimeFormatOptions): string {
  return new Intl.DateTimeFormat('pt-BR', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric',
    ...options,
  }).format(new Date(date))
}

export function formatDateTime(date: string | Date): string {
  return new Intl.DateTimeFormat('pt-BR', {
    day: '2-digit', month: '2-digit', year: 'numeric',
    hour: '2-digit', minute: '2-digit',
  }).format(new Date(date))
}

export function formatCurrency(value: number): string {
  return new Intl.NumberFormat('pt-BR', {
    style: 'currency',
    currency: 'BRL',
  }).format(value)
}

export function formatNumber(value: number, decimals = 2): string {
  return new Intl.NumberFormat('pt-BR', {
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  }).format(value)
}
```