/**
 * Shared — utilitários genéricos reutilizáveis em qualquer contexto.
 *
 *   - useArrayNavigation: navegação (prev/next/jump) em arrays reativos genéricos
 *   - shouldShowDeleteConfirm: decide se deve exibir confirmação antes de deletar
 */
export * from './useArrayNavigation';
export * from './usePlanogramUtils';
