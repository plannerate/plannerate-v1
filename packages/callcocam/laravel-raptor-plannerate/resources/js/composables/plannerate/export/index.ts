/**
 * Export — geração de PDF e captura de canvas para exportação do planograma.
 *
 *   - useCanvasCapture: captura elementos DOM como canvas/imagem (base64/blob)
 *   - usePdfGenerator: gera PDF do planograma a partir da captura do canvas
 */
export * from './useCanvasCapture';
export * from './usePdfGenerator';
