services:
  prometheus:
    image: prom/prometheus:v3.4.2
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=${PROMETHEUS_RETENTION:-15d}'
      - '--web.enable-lifecycle'
      - '--web.external-url=https://${PROMETHEUS_DOMAIN}'
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus_data:/prometheus
    networks:
      - monitoring
      - traefik-global
    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=traefik-global'
      - 'traefik.http.routers.${APP_SLUG:-staging}-prometheus.rule=Host(`${PROMETHEUS_DOMAIN}`)'
      - 'traefik.http.routers.${APP_SLUG:-staging}-prometheus.entrypoints=websecure'
      - 'traefik.http.routers.${APP_SLUG:-staging}-prometheus.tls=true'
      - 'traefik.http.routers.${APP_SLUG:-staging}-prometheus.tls.certresolver=letsencrypt'
      - 'traefik.http.routers.${APP_SLUG:-staging}-prometheus.priority=14'
      - 'traefik.http.services.${APP_SLUG:-staging}-prometheus.loadbalancer.server.port=9090'

  alertmanager:
    image: prom/alertmanager:v0.28.1
    restart: unless-stopped
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=https://${ALERTMANAGER_DOMAIN}'
    volumes:
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager_data:/alertmanager
    networks:
      - monitoring
      - traefik-global
    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=traefik-global'
      - 'traefik.http.routers.${APP_SLUG:-staging}-alertmanager.rule=Host(`${ALERTMANAGER_DOMAIN}`)'
      - 'traefik.http.routers.${APP_SLUG:-staging}-alertmanager.entrypoints=websecure'
      - 'traefik.http.routers.${APP_SLUG:-staging}-alertmanager.tls=true'
      - 'traefik.http.routers.${APP_SLUG:-staging}-alertmanager.tls.certresolver=letsencrypt'
      - 'traefik.http.routers.${APP_SLUG:-staging}-alertmanager.priority=14'
      - 'traefik.http.services.${APP_SLUG:-staging}-alertmanager.loadbalancer.server.port=9093'

  grafana:
    image: grafana/grafana:12.0.2
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_SERVER_ROOT_URL=https://${GRAFANA_DOMAIN}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
    networks:
      - monitoring
      - traefik-global
    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=traefik-global'
      - 'traefik.http.routers.${APP_SLUG:-staging}-grafana.rule=Host(`${GRAFANA_DOMAIN}`)'
      - 'traefik.http.routers.${APP_SLUG:-staging}-grafana.entrypoints=websecure'
      - 'traefik.http.routers.${APP_SLUG:-staging}-grafana.tls=true'
      - 'traefik.http.routers.${APP_SLUG:-staging}-grafana.tls.certresolver=letsencrypt'
      - 'traefik.http.routers.${APP_SLUG:-staging}-grafana.priority=15'
      - 'traefik.http.services.${APP_SLUG:-staging}-grafana.loadbalancer.server.port=3000'

  node-exporter:
    image: prom/node-exporter:v1.9.1
    restart: unless-stopped
    command:
      - '--path.rootfs=/host'
    volumes:
      - /:/host:ro,rslave
    networks:
      - monitoring

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.52.0
    restart: unless-stopped
    privileged: true
    devices:
      - /dev/kmsg:/dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    networks:
      - monitoring

  blackbox-exporter:
    image: prom/blackbox-exporter:v0.27.0
    restart: unless-stopped
    command:
      - '--config.file=/etc/blackbox_exporter/config.yml'
    volumes:
      - ./blackbox.yml:/etc/blackbox_exporter/config.yml:ro
    networks:
      - monitoring

networks:
  monitoring:
    driver: bridge
  traefik-global:
    external: true

volumes:
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
  alertmanager_data:
    driver: local
