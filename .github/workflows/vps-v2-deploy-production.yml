name: vps-v2-deploy-production

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Production deploy is intentionally disabled
        run: |
          echo "Production deploy is disabled in this cycle."
          echo "Planned future path: /opt/plannerate/production"
